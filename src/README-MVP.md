AI Copier Doctor MVP

The existing design/source is preserved. This branch is reserved for the MVP demo flow: upload print sample, select brand/model, run demo diagnosis, view report, and store recent history locally.

Next implementation step: replace the current app entrypoint with the MVP integration after verifying the existing source SHA, then connect the Vercel deployment to the main branch.